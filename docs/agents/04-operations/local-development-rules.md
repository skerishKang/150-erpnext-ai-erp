# Local Development Rules

## Working Directory

```
G:\Ddrive\BatangD\task\workdiary\150-erpnext-ai-erp
```

## Environment

- **OS:** Windows 10 Pro
- **Shell:** Bash (Git Bash or WSL)
- **IDE:** VSCode / Cursor

## Local Setup

### Prerequisites (Future)

- Python 3.10+
- Node.js 18+
- Docker Desktop
- Git

### Development Workflow

1. Open project in VSCode/Cursor
2. Use integrated terminal (bash)
3. Run tests before committing
4. Use `git status` before every commit

## File Paths

- Use forward slashes in git and documentation
- Windows paths for local commands
- Relative paths from project root when possible

## Rules

- Do not install ERPNext yet (documentation phase only)
- Do not generate application code yet (documentation phase only)
- Keep the working directory clean
- Use `.gitignore` for generated files
- Commit documentation changes frequently
