# Static Validation Trigger Check

This docs-only file is used to verify that the repository's GitHub Actions static validation workflow triggers on pull requests.

Related issue: #128

No runtime code is changed.
No external AI call is made.
No credentials are required.
